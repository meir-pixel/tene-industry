# ── Priority ERP ──────────────────────────────────────────────────
PRIORITY_BASE_URL=https://your-company.priorityerp.com
PRIORITY_USER=api_user
PRIORITY_PASS=api_password
PRIORITY_COMPANY=your_company_name

# ── WhatsApp Business API (Meta) ──────────────────────────────────
WHATSAPP_TOKEN=your_whatsapp_token
WHATSAPP_PHONE_ID=your_phone_number_id
WHATSAPP_VERIFY_TOKEN=your_verify_token

# ── Google Cloud Vision (OCR) ─────────────────────────────────────
GOOGLE_APPLICATION_CREDENTIALS=./google-credentials.json
# OR use API key:
GOOGLE_VISION_API_KEY=your_google_vision_api_key

# ── Email (IMAP) ──────────────────────────────────────────────────
EMAIL_IMAP_HOST=imap.gmail.com
EMAIL_IMAP_PORT=993
EMAIL_IMAP_USER=ironbend@your-domain.com
EMAIL_IMAP_PASS=your_app_password
EMAIL_POLL_INTERVAL_MS=60000

# ── Server ────────────────────────────────────────────────────────
PORT=3000
NODE_ENV=production
SESSION_SECRET=החלף_במחרוזת_אקראית_ארוכה_לפחות_32_תווים

# ── Database & Backup ─────────────────────────────────────────────
# DB_PATH – נתיב לקובץ SQLite (ברירת מחדל: ./ironbend.db)
JWT_SECRET=replace_with_a_random_secret_of_at_least_64_characters
BCRYPT_ROUNDS=10
REFRESH_TOKEN_DAYS=7
# Keep false until the frontend token rollout has passed Gate 1a verification.
AUTH_ENFORCEMENT=false
# Production refuses to create a blank DB unless this is intentionally enabled for the first initialization.
ALLOW_EMPTY_DB_INIT=false
# Keep database replacement disabled except during a supervised maintenance window.
ALLOW_DATABASE_UPLOAD=false

DB_PATH=./ironbend.db
# BACKUP_DIR – תיקיית גיבויים יומית (ברירת מחדל: ./backups)
BACKUP_DIR=./backups

# ── Gemini AI ────────────────────────────────────────────────────
GEMINI_API_KEY=your_gemini_api_key
